- import ddox.entities;

- void navModuleList(Package pack)
	- if( pack !is info.rootPackage )
		li= pack.name
	ul
		- foreach(p; pack.packages)
			- navModuleList(p);
		- foreach(m; pack.modules)
			li
				a(href="#{info.rootDir}#{m.qualifiedName}/")= m.name

- navModuleList(info.rootPackage);